---
description: 优先代码质量而非速度；任务过重时拆分为小步依次完成
alwaysApply: true
---

# 质量优先与任务拆分

## 原则

- **默认优先质量**：可读性、与仓库现有风格一致、边界条件与错误处理到位；不为“快”牺牲正确性与可维护性。
- **主动拆分**：当变更跨多模块、含未知依赖或风险高时，拆成可独立验证的小任务，**按序完成**并在每步后自检（编译/静态检查/关键路径）。
- **范围克制**：只做用户请求范围内的改动；避免顺带大重构；确有必要时先说明再动。

## 自检（编码完成后快速过一遍）

- 是否与 `MEMORY.md`、项目分层与 CRUD 规范一致
- 是否引入重复逻辑或可收敛的公共方法
- 是否需要补充错误码/校验/事务边界（写操作）
