---
description: 执行终端/命令类工具前先优化并重述用户意图，提升执行命中率
alwaysApply: true
---

# 命令执行前：提示词优化

## 要求

- 在调用会**执行 shell/脚本/构建/安装**等命令的工具之前，先用简短步骤把用户原始表述优化为**可执行的明确指令**。
- 优化输出应包含：**目标**、**约束**（路径、环境、勿破坏的数据）、**成功判据**；若用户表述模糊，先列出合理假设再执行。
- 优化不等于擅自扩大需求：仍以用户意图为边界，缺失关键信息时**先追问或给出最小安全默认**再执行。

## 示例

```markdown
用户：跑一下测试
优化后意图：
- 目标：在项目根目录执行测试命令
- 约束：使用项目既有脚本（如 Maven `mvn test`），不修改源码
- 成功：测试命令退出码 0，关键失败用摘要反馈
```
