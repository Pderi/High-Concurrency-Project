---
description: 动手改代码前先给出实现方案，经用户明确同意后再编写或修改代码
alwaysApply: true
---

# 方案先行，确认后再编码

## 流程

1. **先出方案**：在首次需要创建或修改代码/配置前，输出实现方案（目标、涉及文件/模块、关键设计点、风险与替代方案）。
2. **等待确认**：在用户**明确表示可以按方案实现**（例如「按这个来」「开始写代码」「OK，实现吧」）之前，**不进行**实质性的代码或配置文件写入（可只做只读检索与说明）。
3. **例外**：用户在同一条消息中已写明可直接实现（如「直接改，不用再问」），或仅请求只读分析/文档/规则而无编码，则无需额外等待。

## 方案最小结构

- 要改什么、为什么
- 怎么做（步骤或模块级说明）
- 如何验证（命令或场景）
- 不确定点（若有）与用户需拍板的选项
