---
description: 三层记忆（热/温/冷）与 ERROR 纠错闭环；生成前后读写 MEMORY/ERROR
alwaysApply: true
---

# 三层记忆与纠错工作流

## 记忆分层

| 层级 | 载体 | 用途 |
|------|------|------|
| 热记忆 | 仓库根目录 `MEMORY.md` | 项目快照、已定业务结论、`docs` 与脚本**索引**；优先阅读以快速对齐上下文 |
| 温记忆 | `docs/` 下文档 | 需求、设计、技术方案等**详述**；按需深入阅读，勿凭空虚构路径 |
| 冷记忆 | 全仓库代码与配置 | 实现真相；以检索与阅读源码为准，与文档冲突时以代码与已采纳结论为准 |

## 纠错机制

- 载体：仓库根目录 `ERROR.md`。
- 记录内容：生成过程中的错误现象、根因、修复动作、预防措施；沿用文件中已有模板与检查清单。

## 强制执行的工作流

1. **生成前**：在开始输出方案、文档或代码前，先阅读 `ERROR.md` 与 `MEMORY.md`（至少覆盖错误清单、项目结论与文档索引），避免重复已记录问题。
2. **生成中**：若需细节，从 `MEMORY.md` 索引进入 `docs/` 或代码，避免与已定边界冲突。
3. **生成后**：在本次任务产生实质变更时，**同步更新**：
   - `MEMORY.md`：文档/脚本新增或重命名、业务结论变化、索引过时、最近更新记录。
   - `ERROR.md`：本次出现的失误与反思（当场记录，不延后）；若同类问题重复出现，补充为检查清单项。

## 示例（生成后应做的事）

```markdown
# MEMORY.md：在「文档索引」加入新文件路径；在「最近更新记录」写一行日期与摘要。
# ERROR.md：按模板追加一条 [YYYY-MM-DD] 记录，状态标为已修复或待修复。
```
